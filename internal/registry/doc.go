// Package registry lists the skill repositories of a GitHub owner or
// organization via the GitHub REST API, so a search can fan out across an
// owner's public repositories. All responses are treated as untrusted input.
package registry
